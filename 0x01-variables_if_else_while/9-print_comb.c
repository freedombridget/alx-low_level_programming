#include <stdio.h>
#include <stdlib.h>

/**
 * main - program for the number
 * numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int ch;

	for (ch = 0; ch <= 9; ch++)
	{
		putchar(ch);
	}
	putchar(", " ch);

	return (0);
}
