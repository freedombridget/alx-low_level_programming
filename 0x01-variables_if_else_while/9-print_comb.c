#include <stdio.h>
#include <stdlib.h>

/**
 * main - program for the number
 * numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i = 0;

	for (i = 0; i <= 9; i++)
	{
		putchar(i);
	}
	putchar(' ');
	putchar('\n');

	return (0);
}
